using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class L : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("From Music folder");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
